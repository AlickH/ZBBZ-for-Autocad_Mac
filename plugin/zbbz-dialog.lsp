(defun zbbz-dialog-bool-to-tile (value)
  (if value "1" "0"))

(setq *zbbz-dialog-initializing* nil)

(defun zbbz-dialog-debug-prompt (text)
  (prompt (strcat "\nZBBZDIALOG " text))
  (zbbz-debug-log (strcat "ZBBZDIALOG " text))
)

(defun zbbz-dialog-set-popup-items (key items / index)
  (start_list key)
  (setq index 0)
  (while (< index (length items))
    (add_list (nth index items))
    (setq index (+ index 1)))
  (end_list))

(defun zbbz-dialog-find-index (items target / index found)
  (setq index 0)
  (setq found nil)
  (while (and (< index (length items)) (null found))
    (if (equal (nth index items) target)
      (setq found index)
      (setq index (+ index 1))))
  (if found found 0))

(defun zbbz-dialog-default-layers ()
  (list "*CURRENT*" "0"))

(defun zbbz-dialog-default-coord-mode-items ()
  (list "Current Coordinate System" "World Coordinate System" "Custom Coordinate System"))

(defun zbbz-dialog-default-prefix-items ()
  (list "XY" "AB" "NE" "None"))

(defun zbbz-dialog-default-behavior-items ()
  (list "Swap X/Y" "Group On" "Auto Orient"))

(defun zbbz-dialog-default-arrow-styles ()
  (list "none"))

(defun zbbz-dialog-default-text-styles ()
  (list "*CURRENT*" "Standard"))

(defun zbbz-dialog-default-precision-items ()
  (list "0.000" "0.00" "0.0" "0" "0.0000"))

(defun zbbz-dialog-sync-preview (/ lines)
  (setq lines
    (zbbz-format-output-lines
      0.0
      0.0
      (zbbz-state-get 'prefix_mode)
      (zbbz-state-get 'swap_xy)
      (zbbz-state-get 'precision)))
  (set_tile "preview_line_1" (car lines))
  (set_tile "preview_line_2" (cadr lines)))

(defun zbbz-dialog-set-list-selection (key indexes / value_text)
  (setq value_text "")
  (foreach index indexes
    (if (= value_text "")
      (setq value_text (itoa index))
      (setq value_text (strcat value_text " " (itoa index)))))
  (set_tile key value_text))

(defun zbbz-dialog-update-custom-input-state (/ mode disabled_mode enabled_mode)
  (setq mode (zbbz-state-get 'coord_mode))
  (setq enabled_mode 0)
  (setq disabled_mode 1)
  (mode_tile "base_n" (if (eq mode 'custom) enabled_mode disabled_mode))
  (mode_tile "base_e" (if (eq mode 'custom) enabled_mode disabled_mode))
  (mode_tile "rotation" (if (eq mode 'custom) enabled_mode disabled_mode))
  (mode_tile "apply_base_angle" (if (eq mode 'custom) enabled_mode disabled_mode))
  (mode_tile "pick_two_points" enabled_mode))

(defun zbbz-dialog-populate ()
  (setq *zbbz-dialog-initializing* T)
  (zbbz-dialog-debug-prompt "populate start")
  (zbbz-dialog-debug-prompt (strcat "coord_mode=" (vl-prin1-to-string (zbbz-state-get 'coord_mode))))
  (zbbz-dialog-debug-prompt (strcat "prefix_mode=" (vl-prin1-to-string (zbbz-state-get 'prefix_mode))))
  (zbbz-dialog-debug-prompt (strcat "precision=" (itoa (zbbz-state-get 'precision))))
  (zbbz-dialog-debug-prompt (strcat "swap_xy=" (vl-prin1-to-string (zbbz-state-get 'swap_xy))))
  (zbbz-dialog-debug-prompt (strcat "group_on=" (vl-prin1-to-string (zbbz-state-get 'group_on))))
  (zbbz-dialog-debug-prompt (strcat "auto_orient=" (vl-prin1-to-string (zbbz-state-get 'auto_orient))))
  (zbbz-dialog-debug-prompt (strcat "background_mask=" (vl-prin1-to-string (zbbz-state-get 'background_mask))))
  (zbbz-dialog-debug-prompt (strcat "base_n=" (rtos (zbbz-state-get 'base_n) 2 6)))
  (zbbz-dialog-debug-prompt (strcat "base_e=" (rtos (zbbz-state-get 'base_e) 2 6)))
  (zbbz-dialog-debug-prompt (strcat "rotation=" (rtos (zbbz-state-get 'rotation) 2 6)))
  (setq coord_mode_items (zbbz-dialog-default-coord-mode-items))
  (zbbz-dialog-set-popup-items "coord_mode" coord_mode_items)
  (setq coord_mode_value
    (cond
      ((eq (zbbz-state-get 'coord_mode) 'world) "World Coordinate System")
      ((eq (zbbz-state-get 'coord_mode) 'custom) "Custom Coordinate System")
      (T "Current Coordinate System")))
  (zbbz-dialog-default-select "coord_mode" (zbbz-dialog-find-index coord_mode_items coord_mode_value))
  (set_tile "base_n" (zbbz-format-number (zbbz-state-get 'base_n) 6))
  (set_tile "base_e" (zbbz-format-number (zbbz-state-get 'base_e) 6))
  (set_tile "rotation" (zbbz-format-number (zbbz-state-get 'rotation) 6))
  (set_tile "arrow_size" (zbbz-format-number (zbbz-state-get 'arrow_size) 6))
  (set_tile "bearing_angle" (zbbz-format-number (zbbz-state-get 'bearing_angle) 6))
  (set_tile "dim_scale" (zbbz-format-number (zbbz-state-get 'dim_scale) 6))
  (set_tile "text_height" (zbbz-format-number (zbbz-state-get 'text_height) 6))
  (set_tile "background_mask" (zbbz-dialog-bool-to-tile (zbbz-state-get 'background_mask)))
  (zbbz-dialog-set-popup-items "behavior_list" (zbbz-dialog-default-behavior-items))
  (setq behavior_indexes nil)
  (if (zbbz-state-get 'swap_xy) (setq behavior_indexes (append behavior_indexes (list 0))))
  (if (zbbz-state-get 'group_on) (setq behavior_indexes (append behavior_indexes (list 1))))
  (if (zbbz-state-get 'auto_orient) (setq behavior_indexes (append behavior_indexes (list 2))))
  (zbbz-dialog-set-list-selection "behavior_list" behavior_indexes)
  (setq prefix_items (zbbz-dialog-default-prefix-items))
  (zbbz-dialog-set-popup-items "prefix_mode" prefix_items)
  (setq prefix_value
    (cond
      ((eq (zbbz-state-get 'prefix_mode) 'ab) "AB")
      ((eq (zbbz-state-get 'prefix_mode) 'ne) "NE")
      ((eq (zbbz-state-get 'prefix_mode) 'none) "None")
      (T "XY")))
  (zbbz-dialog-default-select "prefix_mode" (zbbz-dialog-find-index prefix_items prefix_value))
  (zbbz-dialog-set-popup-items "dim_layer" (zbbz-dialog-default-layers))
  (zbbz-dialog-default-select "dim_layer" 0)
  (zbbz-dialog-set-popup-items "arrow_style" (zbbz-dialog-default-arrow-styles))
  (zbbz-dialog-default-select "arrow_style" 0)
  (zbbz-dialog-set-popup-items "text_style" (zbbz-dialog-default-text-styles))
  (zbbz-dialog-default-select "text_style" 0)
  (setq precision_items (zbbz-dialog-default-precision-items))
  (zbbz-dialog-set-popup-items "precision" precision_items)
  (setq precision_value
    (cond
      ((= (zbbz-state-get 'precision) 4) "0.0000")
      ((= (zbbz-state-get 'precision) 2) "0.00")
      ((= (zbbz-state-get 'precision) 1) "0.0")
      ((= (zbbz-state-get 'precision) 0) "0")
      (T "0.000")))
  (zbbz-dialog-default-select "precision" (zbbz-dialog-find-index precision_items precision_value))
  (zbbz-dialog-debug-prompt (strcat "coord_mode tile=" (get_tile "coord_mode")))
  (zbbz-dialog-debug-prompt (strcat "prefix_mode tile=" (get_tile "prefix_mode")))
  (zbbz-dialog-debug-prompt (strcat "precision tile=" (get_tile "precision")))
  (zbbz-dialog-debug-prompt (strcat "behavior_list tile=" (get_tile "behavior_list")))
  (zbbz-dialog-debug-prompt (strcat "background_mask tile=" (get_tile "background_mask")))
  (zbbz-dialog-update-custom-input-state)
  (zbbz-dialog-sync-preview)
  (setq *zbbz-dialog-initializing* nil))

(defun zbbz-dialog-default-select (key index)
  (set_tile key (itoa index)))

(defun zbbz-dialog-save-edit-number (tile key / value)
  (setq value (distof (get_tile tile) 2))
  (if value
    (zbbz-state-put key value)))

(defun zbbz-dialog-save-behavior-list (/ selected_values)
  (setq selected_values (get_tile "behavior_list"))
  (zbbz-state-put 'swap_xy (if (wcmatch selected_values "*0*") T nil))
  (zbbz-state-put 'group_on (if (wcmatch selected_values "*1*") T nil))
  (zbbz-state-put 'auto_orient (if (wcmatch selected_values "*2*") T nil)))

(defun zbbz-dialog-save-prefix-mode ()
  (cond
    ((= (atoi (get_tile "prefix_mode")) 1) (zbbz-state-put 'prefix_mode 'ab))
    ((= (atoi (get_tile "prefix_mode")) 2) (zbbz-state-put 'prefix_mode 'ne))
    ((= (atoi (get_tile "prefix_mode")) 3) (zbbz-state-put 'prefix_mode 'none))
    (T (zbbz-state-put 'prefix_mode 'xy))))

(defun zbbz-dialog-save-coord-mode ()
  (cond
    ((= (atoi (get_tile "coord_mode")) 1) (zbbz-state-put 'coord_mode 'world))
    ((= (atoi (get_tile "coord_mode")) 2) (zbbz-state-put 'coord_mode 'custom))
    (T (zbbz-state-put 'coord_mode 'current))))

(defun zbbz-dialog-save ()
  (if (not *zbbz-dialog-initializing*)
    (progn
      (zbbz-dialog-save-coord-mode)
      (zbbz-dialog-save-edit-number "base_n" 'base_n)
      (zbbz-dialog-save-edit-number "base_e" 'base_e)
      (zbbz-dialog-save-edit-number "rotation" 'rotation)
      (zbbz-dialog-save-edit-number "arrow_size" 'arrow_size)
      (zbbz-dialog-save-edit-number "bearing_angle" 'bearing_angle)
      (zbbz-dialog-save-edit-number "dim_scale" 'dim_scale)
      (zbbz-dialog-save-edit-number "text_height" 'text_height)
      (zbbz-dialog-save-behavior-list)
      (zbbz-state-put 'background_mask (= (get_tile "background_mask") "1"))
      (setq precision_index (atoi (get_tile "precision")))
      (zbbz-state-put 'precision
        (cond
          ((= precision_index 0) 3)
          ((= precision_index 1) 2)
          ((= precision_index 2) 1)
          ((= precision_index 3) 0)
          ((= precision_index 4) 4)
          (T 3)))
      (zbbz-dialog-save-prefix-mode))))

(defun zbbz-dialog-request-action (action_code)
  (zbbz-dialog-save)
  (setq *zbbz-dialog-action* action_code)
  (done_dialog 2))

(defun zbbz-dialog-handle-apply-base-angle ()
  (zbbz-dialog-save)
  (zbbz-state-apply-base-angle
    (zbbz-state-get 'base_n)
    (zbbz-state-get 'base_e)
    (zbbz-state-get 'rotation))
  (zbbz-dialog-populate))

(defun zbbz-dialog-bind-actions ()
  (action_tile "coord_mode" "(if (not *zbbz-dialog-initializing*) (progn (zbbz-dialog-save-coord-mode) (zbbz-dialog-update-custom-input-state)))")
  (action_tile "behavior_list" "(if (not *zbbz-dialog-initializing*) (progn (zbbz-dialog-save-behavior-list) (zbbz-dialog-sync-preview)))")
  (action_tile "background_mask" "(if (not *zbbz-dialog-initializing*) (zbbz-state-put 'background_mask (= $value \"1\")))")
  (action_tile "prefix_mode" "(if (not *zbbz-dialog-initializing*) (progn (zbbz-dialog-save-prefix-mode) (zbbz-dialog-sync-preview)))")
  (action_tile "apply_base_angle" "(zbbz-dialog-handle-apply-base-angle)")
  (action_tile "pick_two_points" "(zbbz-dialog-request-action 'pick_two_points)")
  (action_tile "draw_grid" "(zbbz-dialog-request-action 'draw_grid)")
  (action_tile "pick_bearing" "(zbbz-dialog-request-action 'pick_bearing)")
  (action_tile "export_dat" "(zbbz-dialog-request-action 'export_dat)")
  (action_tile "help" "(zbbz-dialog-request-action 'help)")
  (action_tile "accept" "(setq *zbbz-dialog-action* 'accept) (zbbz-dialog-save) (done_dialog 1)")
  (action_tile "cancel" "(done_dialog 0)"))

(defun zbbz-dialog-run-once (/ dialog_id result)
  (setq dialog_id (load_dialog (zbbz-plugin-path "zbbz.dcl")))
  (if (< dialog_id 0)
    (progn
      (prompt "\nUnable to load zbbz.dcl.")
      nil)
    (progn
      (if (not (new_dialog "SetDimZB" dialog_id))
        (progn
          (prompt "\nUnable to open SetDimZB dialog.")
          (setq result 0))
        (progn
          (setq *zbbz-dialog-action* 'accept)
          (zbbz-dialog-populate)
          (zbbz-dialog-bind-actions)
          (setq result (start_dialog))))
      (unload_dialog dialog_id)
      (list result *zbbz-dialog-action*))))

(defun zbbz-dialog-open-loop (/ dialog_result dialog_code action_value bearing_angle calibration)
  (setq action_value 'accept)
  (while action_value
    (setq dialog_result (zbbz-dialog-run-once))
    (if (null dialog_result)
      (setq action_value nil)
      (progn
        (setq dialog_code (car dialog_result))
        (setq action_value (cadr dialog_result))
        (cond
          ((= dialog_code 0)
            (setq action_value nil)
            nil)
          ((= dialog_code 1)
            (setq action_value nil)
            T)
          ((eq action_value 'pick_bearing)
            (setq bearing_angle (zbbz-pick-bearing-angle))
            (if bearing_angle
              (zbbz-state-put 'bearing_angle bearing_angle)))
          ((eq action_value 'pick_two_points)
            (setq calibration (zbbz-pick-two-point-calibration))
            (if calibration
              (zbbz-pick-apply-calibration calibration)))
          ((eq action_value 'draw_grid)
            (zbbz-grid-draw))
          ((eq action_value 'export_dat)
            (zbbz-dat-export-session))
          ((eq action_value 'help)
            (zbbz-help-show))
          (T
            (setq action_value nil)
            nil))))))
(setq *zbbz-dialog-action* 'accept)
